import React from 'react';
import { Link } from 'react-router-dom';

const Subscription = () => {
    return (
        <div>
            Subscription page : <Link to='/make-payment'>Make Payment</Link>
        </div>
    );
};

export default Subscription;